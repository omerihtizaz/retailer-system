apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "retailer-system.fullname" . }}-test-connection"
  labels:
    {{- include "retailer-system.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "retailer-system.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
